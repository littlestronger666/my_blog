# 立即修复 - 快速步骤

## 🚀 3 步解决问题

### 步骤 1: 更新 CNAME 文件（30秒）

打开 `public/CNAME` 文件，将内容改为你的域名：

```
你的域名.com
```

例如：
```
blog.example.com
```

或：
```
example.com
```

**注意：不要包含 https:// 或 www.**

### 步骤 2: 拉取并推送（1分钟）

在项目目录执行：

```bash
# 拉取远程更改
git pull origin main --rebase

# 添加所有更改
git add .

# 提交
git commit -m "Fix custom domain configuration"

# 推送
git push origin main
```

**如果主分支是 master，将 main 改为 master**

### 步骤 3: 等待部署（2-3分钟）

1. 访问 GitHub 仓库的 Actions 标签
2. 等待构建完成（绿色勾号）
3. 访问你的域名
4. 强制刷新（Ctrl+Shift+R）

## ⚠️ 如果 git pull 失败

执行：

```bash
git pull origin main --allow-unrelated-histories
```

然后继续步骤 2 的其他命令。

## ⚠️ 如果还是推送失败

使用强制推送（会覆盖远程更改）：

```bash
git push origin main --force
```

## ✅ 完成！

推送成功后，等待 2-3 分钟，访问你的域名查看效果。

## 🔍 检查配置

确认以下配置正确：

1. **next.config.ts**
   ```typescript
   const REPO_NAME = ''; // 必须是空字符串
   ```

2. **public/CNAME**
   ```
   你的域名.com  // 只写域名，不要其他内容
   ```

3. **GitHub Pages 设置**
   - Settings → Pages → Custom domain
   - 填写你的域名
   - 勾选 Enforce HTTPS

## 💡 快速命令（复制粘贴）

```bash
git pull origin main --rebase && git add . && git commit -m "Fix deployment" && git push origin main
```

如果失败，使用：

```bash
git pull origin main --allow-unrelated-histories && git add . && git commit -m "Fix deployment" && git push origin main
```
