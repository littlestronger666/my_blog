# 解决 GitHub 连接问题

## 🚨 错误信息

```
fatal: unable to access 'https://github.com/littlestronger666/my_blog.git/': 
Failed to connect to github.com port 443 after 21050 ms: Could not connect to server
```

## 🔍 原因

这是网络连接问题，可能的原因：
1. 网络代理设置问题
2. 防火墙阻止
3. DNS 解析问题
4. GitHub 服务暂时不可用

## ✅ 解决方案

### 方案 1: 检查并配置代理（最常见）

如果你使用了代理软件（如 V2Ray、Clash 等）：

```bash
# 设置 Git 代理（假设代理端口是 7890）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 然后重试
git pull origin main --rebase
```

**查看你的代理端口：**
- 打开你的代理软件
- 查看 HTTP 代理端口（通常是 7890、1080 或 10809）
- 将上面命令中的 7890 改成你的端口

### 方案 2: 取消代理（如果不需要代理）

```bash
# 取消 Git 代理
git config --global --unset http.proxy
git config --global --unset https.proxy

# 然后重试
git pull origin main --rebase
```

### 方案 3: 使用 SSH 而不是 HTTPS

```bash
# 1. 查看当前远程地址
git remote -v

# 2. 改为 SSH 地址
git remote set-url origin git@github.com:littlestronger666/my_blog.git

# 3. 重试
git pull origin main --rebase
```

**注意：使用 SSH 需要先配置 SSH 密钥**

### 方案 4: 修改 hosts 文件（DNS 问题）

如果是 DNS 解析问题，可以修改 hosts 文件：

**Windows:**
1. 以管理员身份打开记事本
2. 打开文件：`C:\Windows\System32\drivers\etc\hosts`
3. 添加以下内容：
```
140.82.113.4 github.com
199.232.69.194 github.global.ssl.fastly.net
```
4. 保存并重试

**Linux/Mac:**
```bash
sudo nano /etc/hosts
# 添加上面的内容
# 保存后重试
```

### 方案 5: 等待并重试

有时是 GitHub 服务暂时不可用：

```bash
# 等待几分钟后重试
git pull origin main --rebase
```

## 🎯 推荐步骤（按顺序尝试）

### 步骤 1: 检查网络

```bash
# 测试能否访问 GitHub
ping github.com

# 如果不通，说明网络有问题
```

### 步骤 2: 配置代理（如果使用代理）

```bash
# 设置代理（改成你的代理端口）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890
```

### 步骤 3: 重试 Git 操作

```bash
git pull origin main --rebase
```

### 步骤 4: 如果成功，继续推送

```bash
git add .
git commit -m "Fix deployment"
git push origin main
```

## 🔧 快速诊断

### 检查代理设置

```bash
# 查看当前 Git 代理配置
git config --global --get http.proxy
git config --global --get https.proxy
```

### 检查网络连接

```bash
# Windows
ping github.com

# 测试 HTTPS 连接
curl -I https://github.com
```

## 💡 常见代理端口

不同代理软件的默认端口：

| 软件 | HTTP 端口 | SOCKS5 端口 |
|------|-----------|-------------|
| Clash | 7890 | 7891 |
| V2Ray | 10809 | 10808 |
| SSR | 1080 | 1080 |

## 🆘 如果所有方法都不行

### 临时方案：直接编辑文件

既然无法 pull，我们可以：

1. **不 pull，直接强制推送**（会覆盖远程）
```bash
git push origin main --force
```

2. **或者在 GitHub 网页上直接编辑**
   - 访问 GitHub 仓库
   - 在网页上编辑 `public/CNAME` 文件
   - 提交更改

3. **或者删除重建**
   - 在 GitHub 上下载仓库的 ZIP
   - 解压后应用你的更改
   - 删除旧仓库，创建新仓库
   - 推送代码

## 📝 完整命令（复制粘贴）

### 如果使用代理（端口 7890）：

```bash
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890
git pull origin main --rebase
git add .
git commit -m "Fix deployment"
git push origin main
```

### 如果不使用代理：

```bash
git config --global --unset http.proxy
git config --global --unset https.proxy
git pull origin main --rebase
git add .
git commit -m "Fix deployment"
git push origin main
```

### 如果还是不行，强制推送：

```bash
git push origin main --force
```

## ⚠️ 注意事项

- 强制推送会覆盖远程的更改
- 如果有其他人协作，不要使用强制推送
- 配置代理后记得在不需要时取消

## 🎉 成功后

推送成功后：
1. 访问 GitHub 仓库的 Actions 标签
2. 等待构建完成
3. 访问你的域名
4. 检查样式是否正常
